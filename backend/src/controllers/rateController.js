export const getRules = async (req, res) => {
  res.status(410).json({
    error: 'Rate controller is no longer supported',
  });
};

export const createRule = async (req, res) => {
  res.status(410).json({
    error: 'Rate controller is no longer supported',
  });
};

export const getPricingHistory = async (req, res) => {
  res.status(410).json({
    error: 'Rate controller is no longer supported',
  });
};
